---
import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import ContactForm from '../components/ContactForm.astro';
import { siteData, socialLinks, contactInfo } from '../data/siteData';
import MapImage from "@assets/photo-1517487881594-2787fef5ebf7.jpg"
---

<Layout title={`Contact Us - ${siteData.name}`} description="Get in touch with us to start your fitness journey">
  <!-- Hero Section -->
  <section class="pt-32 pb-20 bg-linear-to-br from-slate-900 via-slate-800 to-slate-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-3xl mx-auto">
        <h1 class="text-5xl md:text-6xl font-bold text-white mb-6">Contactanos</h1>
        <p class="text-xl text-gray-300">
          Estamos aquí para ayudarte en tu viaje de transporte o comenzar tú negocio. ¡Contáctanos hoy!
        </p>
      </div>
    </div>
  </section>

  <!-- Contact Info & Form -->
  <section class="py-20 bg-slate-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
        <!-- Contact Information -->
        <div>
          <h2 class="text-3xl font-bold text-white mb-8">Ponte en contacto</h2>
          <div class="space-y-6">
            {contactInfo.map((info) => (
              <div class="flex items-start gap-4">
                <div class="bg-orange-500 p-4 rounded-lg shrink-0">
                  <i class={`${info.icon} text-2xl text-white`}></i>
                </div>
                <div>
                  <h3 class="text-xl font-semibold text-white mb-2">{info.title}</h3>
                  {Array.isArray(info.content) ? (
                    info.content.map((line) => (
                      <p class="text-gray-400 leading-relaxed">{line}</p>
                    ))
                  ) : info.link ? (
                    <a href={info.link} class="text-gray-400 hover:text-orange-500 transition-colors">
                      {info.content}
                    </a>
                  ) : (
                    <p class="text-gray-400 leading-relaxed">{info.content}</p>
                  )}
                </div>
              </div>
            ))}
          </div>

          <!-- Social Links -->
          <div class="mt-10">
            <h3 class="text-xl font-semibold text-white mb-4">Siguenos</h3>
            <div class="flex space-x-4">
              {socialLinks.map((social) => (
                <a
                  href={siteData.social[social.name as keyof typeof siteData.social]}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="w-12 h-12 bg-slate-700 hover:bg-orange-500 rounded-lg flex items-center justify-center transition-all"
                  aria-label={social.label}
                >
                  <i class={`${social.icon} text-xl text-white`}></i>
                </a>
              ))}
            </div>
          </div>
        </div>

        <!-- Contact Form -->
        <div>
          <ContactForm />
        </div>
      </div>
    </div>
  </section>

  <!-- Visit Expectations Section -->
  <section class="py-20 bg-slate-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-white mb-4 text-center">
        ¿Qué puedes esperar al contactarnos?
      </h2>
      <p class="text-xl text-gray-300 mb-12 text-center max-w-2xl mx-auto">
        Te acompañamos desde la idea inicial hasta la entrega de tu remolque.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto mb-12">

        <!-- Paso 1 -->
        <div class="bg-slate-800 p-6 rounded-xl border border-slate-700 text-center hover:border-orange-500 transition-all">
          <div class="w-16 h-16 bg-orange-500/10 rounded-lg flex items-center justify-center mx-auto mb-4">
            <i class="ri-user-search-line text-3xl text-orange-500"></i>
          </div>
          <h3 class="text-xl font-bold text-white mb-3">Asesoría Inicial</h3>
          <p class="text-gray-300 text-sm leading-relaxed">
            Escuchamos tu idea y analizamos el tipo de remolque que necesitas según tu proyecto.
          </p>
        </div>

        <!-- Paso 2 -->
        <div class="bg-slate-800 p-6 rounded-xl border border-slate-700 text-center hover:border-orange-500 transition-all">
          <div class="w-16 h-16 bg-orange-500/10 rounded-lg flex items-center justify-center mx-auto mb-4">
            <i class="ri-group-line text-3xl text-orange-500"></i>
          </div>
          <h3 class="text-xl font-bold text-white mb-3">Diseño y Planeación</h3>
          <p class="text-gray-300 text-sm leading-relaxed">
            Definimos medidas, materiales y acabados para crear un remolque a la medida.
          </p>
        </div>

        <!-- Paso 3 -->
        <div class="bg-slate-800 p-6 rounded-xl border border-slate-700 text-center hover:border-orange-500 transition-all">
          <div class="w-16 h-16 bg-orange-500/10 rounded-lg flex items-center justify-center mx-auto mb-4">
            <i class="ri-file-list-3-line text-3xl text-orange-500"></i>
          </div>
          <h3 class="text-xl font-bold text-white mb-3">Cotización y Fabricación</h3>
          <p class="text-gray-300 text-sm leading-relaxed">
            Te entregamos una cotización clara y comenzamos la fabricación de tu remolque.
          </p>
        </div>

      </div>
    </div>
  </section>


  <!-- Response Time Section -->
  <section class="py-20 bg-slate-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto">
        <div class="bg-slate-900 p-8 rounded-xl border border-slate-700">
          <div class="flex items-start gap-6">
            <div class="bg-orange-500/10 p-4 rounded-lg shrink-0">
              <i class="ri-time-line text-4xl text-orange-500"></i>
            </div>
            <div>
              <h3 class="text-2xl font-bold text-white mb-3">Tiempo de respuesta</h3>
              <p class="text-gray-300 mb-4 leading-relaxed">
                Nos comprometemos a atender tu solicitud lo más pronto posible. Esto es lo que puedes esperar:
              </p>
              <ul class="space-y-3">
                <li class="flex items-start gap-3">
                  <i class="ri-checkbox-circle-fill text-orange-500 text-xl mt-0.5"></i>
                  <div>
                    <span class="text-white font-semibold">Mensajes y formularios:</span>
                    <span class="text-gray-300"> Respuesta inicial en un plazo máximo de 24 horas hábiles</span>
                  </div>
                </li>
                <li class="flex items-start gap-3">
                  <i class="ri-checkbox-circle-fill text-orange-500 text-xl mt-0.5"></i>
                  <div>
                    <span class="text-white font-semibold">WhatsApp y llamadas:</span>
                    <span class="text-gray-300"> Atención directa en horario laboral</span>
                  </div>
                </li>
                <li class="flex items-start gap-3">
                  <i class="ri-checkbox-circle-fill text-orange-500 text-xl mt-0.5"></i>
                  <div>
                    <span class="text-white font-semibold">Cotizaciones:</span>
                    <span class="text-gray-300"> Te enviamos una propuesta clara y detallada en el menor tiempo posible</span>
                  </div>
                </li>
                <li class="flex items-start gap-3">
                  <i class="ri-checkbox-circle-fill text-orange-500 text-xl mt-0.5"></i>
                  <div>
                    <span class="text-white font-semibold">Proyectos urgentes:</span>
                    <span class="text-gray-300"> Contáctanos directamente para atención prioritaria</span>
                  </div>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>


  <!-- Map Section -->
<section class="py-20 bg-slate-900">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-white mb-12 text-center">
      Encuéntranos
    </h2>

    <div class="max-w-5xl mx-auto bg-slate-800 p-4 rounded-xl border border-slate-700">
      <div class="aspect-video rounded-lg overflow-hidden">
        <iframe
          src="https://www.google.com/maps/embed?pb=!1m14!1m12!1m3!1d331.8505153040217!2d-99.0912921149743!3d19.7780847473474!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!5e0!3m2!1ses!2smx!4v1766866020386!5m2!1ses!2smx"
          class="w-full h-full border-0"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          allowfullscreen
        ></iframe>
      </div>
    </div>
  </div>
</section>

</Layout>

